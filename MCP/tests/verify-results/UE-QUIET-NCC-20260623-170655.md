# UE 5.6 verification — cortex-gems

**Verdict: PASS** (compile + functional) on UE 5.6.1.

- **Host:** QUIET-NCC
- **Date:** 2026-06-23
- **Engines:** launcher `UE_5.6` (5.6.1, CL 43139311) for the functional host build; source `UnrealEngine-5-6-1` (5.6.1) for an independent compile check.
- **Branch verified:** `upstream-pr/cortex-gems` (functional run was on `sync/cortex-gems-0.1.13` = this branch + one UE 5.4 non-unity include fix; that fix is orthogonal to 5.6 and to the feature set).

## Compile (the real check) — PASS, two independent ways
1. **Source 5.6.1**, plugin built into a minimal blank C++ host project (`CortexHostEditor`, project-plugin context): **Result: Succeeded**, all 14 Cortex modules incl. `CortexAnimation` compiled + linked, 0 errors.
2. **Launcher 5.6.1**, plugin in a real Lyra project (`LyraEditor`): **Result: Succeeded** (503 actions), all Cortex modules incl. `CortexAnimation.dll` + `CortexEditor.dll` linked, 0 errors.

The version-sensitive animation APIs flagged in `VERIFY_CORTEX_GEMS.md` (`IAnimationDataController` curves, `Notifies` authoring, AnimGraph state-machine introspection) all compile cleanly on 5.6.1.

## Functional — `pytest tests/test_cortex_gems_e2e.py` → **14 passed** (cortex-mcp 0.1.5)
Live Lyra 5.6.1 editor. No skips (Lyra has skeletons + sequences).
- `run_python`: basic, output capture, **defer/next-tick path**, error reporting, missing-param guard — 5/5
- CVars: get, unknown-errors, set round-trip, list — 4/4
- Anim Phase A: list_assets, get_skeleton_info, get_sequence_info — 3/3
- Anim Phase B: socket round-trip, float-curve round-trip (undo-wrapped, self-cleaning) — 2/2

## Notes for maintainers (non-blocking)
1. **`StructUtils` dependency**: the only build warning on 5.6.1 — *"plugin 'StructUtils' was deprecated in 5.5 and will soon be removed."* `.uplugin` should drop/replace it before a future engine removes it.
2. **Test-type `UPROPERTY` Category**: builds clean as a *project* plugin (5.4.4 and 5.6.1), but `RunUAT BuildPlugin` (engine-module strictness) errors on missing `Category=` in `CortexBPTestLiftActor.h`, `CortexSerializerDeepReadTestTypes.h`, `CortexTestDataAsset.h`. Add explicit `Category=` to those test headers if the release flow packages via `BuildPlugin`.
